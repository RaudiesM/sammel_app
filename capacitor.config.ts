import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mi_datastructure',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
